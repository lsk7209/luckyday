# DreamScope 환경 변수 예제
# 이 파일을 .env.local로 복사해서 실제 값들을 설정하세요

# =================================================================
# CLOUDFLARE WORKERS API 설정
# =================================================================
# Cloudflare Workers API URL (꿈 해몽 데이터 API)
NEXT_PUBLIC_WORKERS_API_URL=https://luckyday-api.workers.dev

# 또는 Pages 배포 URL이 있는 경우 자동 변환됨
NEXT_PUBLIC_SITE_URL=https://luckyday.pages.dev

# =================================================================
# OPENAI 설정 (AI 해몽)
# =================================================================
# OpenAI API 키
OPENAI_API_KEY=your-openai-api-key

# OpenAI 모델 설정 (선택사항)
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_MAX_TOKENS=1000

# =================================================================
# CLOUDFLARE 설정 (배포용)
# =================================================================
# Cloudflare API 토큰 (배포용)
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token

# Cloudflare 계정 ID
CLOUDFLARE_ACCOUNT_ID=your-account-id

# =================================================================
# NEXT.JS 설정
# =================================================================
# Next.js 기본 URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# 프로덕션 URL (배포 후 설정)
NEXT_PUBLIC_PRODUCTION_URL=https://your-domain.com

# =================================================================
# 기타 설정
# =================================================================
# 환경
NODE_ENV=development

# 개발 모드에서 상세 에러 표시
NEXT_PUBLIC_DEBUG=false

# 광고 설정 (AdSense)
NEXT_PUBLIC_ADSENSE_ID=ca-pub-xxxxxxxxxxxxxxxx

# 분석 설정 (Google Analytics)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# =================================================================
# 선택사항 설정들
# =================================================================

# Redis URL (캐싱용, 선택사항)
REDIS_URL=redis://localhost:6379

# 이메일 설정 (알림용, 선택사항)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# 웹훅 설정 (Slack 등, 선택사항)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# 로그 레벨
LOG_LEVEL=info
